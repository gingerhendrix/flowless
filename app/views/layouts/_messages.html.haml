- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "alert alert-#{name == :notice ? "success" : name} alert-dismissable"}
      %button.close{ type: 'button', data: { dismiss: 'alert' }, aria: { hidden: 'true' } } &times;
      %div{ id: "flash_#{name}" }= msg