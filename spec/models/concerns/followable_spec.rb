require 'spec_helper'

describe Followable do
  # let (:class_type) { Struct.new(:user) }
  # let (:user)       { FactoryGirl.build(:user) }
  # let (:test_class) { class_type { include Followable } }
  # let (:entity)     { test_class.new(user) }

  # context 'scope' do
  #   before :each do
  #     class_type.stub(:embeds_many)
  #     class_type.stub(:scope)
  #     class_type.stub(:where)
  #   end

  #   describe 'followed_by, remindable_for_follower' do
  #     it { binding.pry; entity.should respond_to :followed_by }
  #     it { entity.should respond_to :remindable_for_follower }
  #   end
  # end

  pending 'figure out a way to test class methods with concerns'
end